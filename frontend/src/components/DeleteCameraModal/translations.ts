export interface DeleteCameraModalTranslations {
    title: string
    description: string
    action: {
        cancel: string
        confirm: string
    }
    notification: {
        success: string
        error: {
            internal: string
        }
    }
}
